# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
GET 	/News									App.News
GET 	/Articles								App.Articles
GET 	/Files									App.Files
GET 	/Photos									App.Photos
GET 	/Videos									App.Videos
GET 	/Profiles								App.ListProfiles
GET 	/ProfilesForm/:id						App.ProfilesForm
GET 	/About									App.About
GET 	/Contact								App.Contact
GET 	/Login									App.Login
GET		/ListProfiles/:page						App.ListProfiles
GET		/ProfilePage/:id						App.ProfilePage
POST	/EditProfiles/:id						App.EditProfiles

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action

# GET     /item/:id   UserCtrl.Get
# POST    /item       UserCtrl.Add
# PUT     /item/:id   UserCtrl.Update
# DELETE  /item/:id   UserCtrl.Delete
# GET     /items      UserCtrl.List
